java 基本数据类型的默认值：
（1）类的成员变量：Java会给一个默认值，如下：
boolean false
char    null
byte    （byte）0
short    （short）0
int      （int）0
long      0L
float     0.0f
double   0.0d

（2）局部变量（非类的成员变量）：
Java不会进行自动初始化，需要手动设置，否则编译时会报出未初始化的错误。
